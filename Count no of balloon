#include <stdio.h>
int main()
{
  int gb,pb,t,p[5],i=0,cp1=0,cp2=0,n=0;
  printf("Enter no of participants: ");
  scanf("%d",&n);
  printf("Enter 0.none 1.p1 2.p2 3.both ");
  for(i=0;i<n;i++);
  {
   scanf("%d",&p[i]);
   if(p[i]=1)
   cp1++;
   else if(p[i]=2)
   cp2++;
   else if (p[i]=3)
   {
     cp1++;
     cp2++;
   }
   else
   continue;
   }
   tot=(cp1*gb)+(cp2*pb);
   printf("Total no of ballon :%d",tot);
}
