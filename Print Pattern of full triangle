#include <stdio.h>
int main()
{
  int i,j,h,s,k;
  printf("Enter Height : ");
  scanf("%d",&h);
  s=h;
  for(i=1;i<=h;i++)
  {
   for(j=1;j<=s-1;j++)
   {
    printf(" ");
   }
   for(k=1;k<=2*i-1;k++)
   { 
    printf("*");
   }
    s--;
    printf("\n");
  }
}
