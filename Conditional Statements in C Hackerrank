#include<stdio.h>

int main()
{
    int n;
    scanf("%d", &n);
    
    char *numbers[10] = {"zero", "one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};
    
    if(n > 9)
        printf("Greater than 9\n");
    else
        printf("%s\n", numbers[n]);
    
    return 0;
}
