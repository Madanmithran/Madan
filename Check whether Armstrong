#include<stdio.h>
int main()
{
int n,on,r,res=0;
printf("Enter a number : ");
scanf("%d",&on);
n=on;
while(on!=0)
{
r=on%10;
res+=r*r*r;
on/=10;
}
if(res==n)
  printf("IT is armstron");
else
 printf("not a armstrong ");
}
